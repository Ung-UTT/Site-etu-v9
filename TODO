À faire :
=========

User :
  += infos via LDAP (avatar, UVs, …) (dump à faire)

Daymail :
  /daymail/send éxécuté par un bot qui a les droits, une fois par jour à 4h du mat'
  News du jour ?

Images, fichiers :
  Images (Users, News, Event, Association, UV ?)
  Annals
  → Paperclip ! (:polymorphic : _id, _type)

Annales
  title, content, uv_id, date, file

Association :
  += logo

Roles :
  += Permissions selon rôle
  += Pouvoir ajouter des membres aux rôles facilement

Via dump à récupérer
  UVs :
    += members_id, schedules_id (has_and_belongs_to_many pour les deux !)

  Planning
    user_id, uvs_id

  Schedule :
    uv_id, location (B201, …), week (A or B), from, to

Category :
  name (:polymorphic) (et :has_many) (pas facile !)

/!\ Facile (mais long) ! /!\
  Repasser en :fr (il faut faire les traduc' des erreurs et du datetime_select)
  Jolis titres pour toute les pages
  Des tests ! Beaucoup de tests !

À préciser :
============

* "J'ai oublié mon mot de passe" : Par ent ? (ou regénérer mot de passe pour compte du site étu)
* Éditeur WYSIWYG : lequel (TinyMCE, Markdown, BBCode, … ?) ?
* Mails : frame vers Roundcube ?
* Préférences utilisateur (dont menu) : quoi ? les cases pour le Daymail, … ?
* Emploi du temps (+ cumul) (relations compliquées)
* BuckUTT : Code BuckUTT → Voir solde, Recharger → (service de payement de la banque ?)
* Forum : phpBB avec le plugin d'authentification qui va bien (?)
