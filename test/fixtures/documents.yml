# Générés car sinon c'est ingérable (taille de fichiers, type, ...)

# FIXME: Trouver comment faire en sorte que les images soient stokées
# en vrai et pas seulement dans la BDD
<% if false %>

<%
  documents = {
    'party_event_1' => {
      'filename' => 'party1.jpg',
      'documentable' => 'party (Event)'
    },
    'party_event_2' => {
      'filename' => 'party2.jpg',
      'documentable' => 'party (Event)'
    },
    'party_event_3' => {
      'filename' => 'party3.jpg',
      'documentable' => 'party (Event)'
    },
  }
%>

<% documents.each do |key, val| %>
<% doc = Document.create(:asset => File.open(Rails.root.join('spec', 'assets', val['filename']))) %>
<%= key %>:
  asset_file_name: <%= doc.asset_file_name %>
  asset_content_type: <%= doc.asset_content_type %>
  asset_file_size: <%= doc.asset_file_size %>
  asset_updated_at: <%= doc.asset_updated_at %>
  documentable: <%= val['documentable'] %>
  type: Document
<% end %>

<% end %>
