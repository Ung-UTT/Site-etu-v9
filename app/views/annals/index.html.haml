%section.content
  = title t('annals.list')

  %ul
    - @annals.each do |annal|
      %li
        = link_to annal, annal

  - if can? :create, Annal
    %ul.actions
      %li= link_to t('common.add'), new_annal_path
