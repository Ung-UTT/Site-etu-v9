<%= title 'Covoiturage : ' + @carpool.location %>

<p>
  <b>Description :</b>
  <%= @carpool.content %>
</p>

<p>
  <b>Date :</b>
  <%= @carpool.date %>
</p>

<p>
  <b>Conducteur ? </b>
  <%= @carpool.is_driver %>
</p>

<p>
  <b>Tags :</b>
  <%= link_to_tags @carpool %>
</p>

<% if can? :update, @carpool %>
  <%= link_to 'Modifier', edit_carpool_path(@carpool) %> |
<% end %>
<% if can? :destroy, @carpool %>
  <%= button_to 'Supprimer', @carpool, :confirm => 'Sûr ?', :method => :delete %> |
<% end %>
<%= link_to 'Liste des covoiturages', carpools_path %>

<%= render 'documents/list_and_add', {:documentable => @carpool, :documents => @documents} %>
<%= render 'comments/list_and_add', {:commentable => @carpool, :comments => @comments} %>
