<%= title t('timesheets.list') %>

<%= form_tag(timesheets_path, :method => :get) do %>
  <%= select_tag :users, users_select, :multiple => true %>
  <%= submit_tag t('timesheets.cumulate') %>
<% end %>

<ul>
<% @timesheets.each do |timesheet| %>
  <li>
    <%= link_to timesheet.range, timesheet %> (<%= link_to timesheet.course.name, timesheet.course %>)
  </li>
<% end %>
</ul>

<%= render 'timesheets/schedule', :timesheets => @timesheets %>

<br />

<% if can? :create, Timesheet %>
  <%= link_to t('common.add'), new_timesheet_path %>
<% end %>
