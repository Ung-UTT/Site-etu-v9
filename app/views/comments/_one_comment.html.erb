<%= link_to '#', [commentable, comment] %>
<% if commentable.class == Course # Commentaires
  link_to comment.user.login, comment.user
    end %> : <%= comment.content %>
<% if can? :destroy, comment %>
  <%= link_to 'X', [commentable, comment], :confirm => 'Sûr ?', :method => :delete %>
<% end %>