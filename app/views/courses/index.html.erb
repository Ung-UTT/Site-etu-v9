<%= title t('courses.list') %>

<ul>
<% @courses.each do |course| %>
  <li>
    <%= link_to course.name, course %></td>
  </li>
<% end %>
</ul>

<br />

<% if can? :create, Course %>
  <%= link_to t('common.add'), new_course_path %>
<% end %>
