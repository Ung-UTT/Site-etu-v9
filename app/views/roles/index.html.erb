<%= title 'Rôles' %>

<ul>
<% @roles.each do |role| %>
  <li>
    <!-- TODO: Traduire le nom du rôle affiché -->
    <%= link_to role.name + (role.association ? " (#{role.association.name})" : ''), role %></td>
  </li>
<% end %>
</ul>

<br />

<% if can? :create, Role %>
  <%= link_to t('common.add'), new_role_path %>
<% end %>
