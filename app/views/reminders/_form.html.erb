<%= form_for(@reminder) do |f| %>
  <%= f.error_messages %>

  <div class="field">
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit t('common.submit') %>
  </div>
<% end %>
