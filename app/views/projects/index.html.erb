<%= title 'Projets' %>

<ul>
<% @projects.each do |project| %>
  <li>
    <strong><%= link_to project.name, project %></strong> :<br />
    <%= project.description %>
  </li>
<% end %>
</ul>

<br />

<% if can? :create, Project %>
  <%= link_to t('common.add'), new_project_path %>
<% end %>
