<%= form_for(@pool) do |f| %>
  <%= f.error_messages %>

  <div class="field">
    <%= f.label :name, t('pools.name') %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description, t('pools.description') %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit t('common.submit') %>
  </div>
<% end %>
